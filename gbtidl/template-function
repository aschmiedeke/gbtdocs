;+
; This function does something everyone would want it do.  This
; explanation is a useful summary of how it all works.
;
; @param in_param {in}{required}{type=a type} Describe the parameter
; here.  You can go on for as long as it takes.  You should be able to
; embed html (if I only knew some).
;
; @param out_param {out}{required}{type=a type} This parameter is set
; by this function.  Describe it here.
;
; @param optional_param {in}{out}{optional}{type=a type} This
; parameter is optional, but when it is present, it is used both as an
; input and as an output parameter.
;
; @keyword mykey {in}{optional}{type=a type}{default=default value} This keyword controls
; something or another and it has a default value when not supplied.
;
; @keyword boolkey {in}{optional}{type=boolean} This keyword is toggle
; that is usually off but can be turned on by setting it on input.
;
; @returns The secret to life the universe and everything.
;
; @examples
;    Describe examples here.  
; <pre>
;    See hdr.pro for an example of this
; </pre>
;
; @uses list other functions it uses here.  May be links.  See hdr.pro.
;
; @version $Id$
;-
function SOME_FUNCTION, in_param, out_param, optional_param, mykey=mykey, boolkey=boolkey
    compile_opt idl2

    ; check arguments

    ; magic happens here

    return, some_result
END
